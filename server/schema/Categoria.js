cube(`Categoria`, {
  sql: `SELECT * FROM dbo.categoria`,
  
  preAggregations: {
    // Pre-Aggregations definitions go here
    // Learn more here: https://cube.dev/docs/caching/pre-aggregations/getting-started
  },
  
  joins: {
    
  },
  
  measures: {
    count: {
      type: `count`,
      drillMembers: [id]
    }
  },
  
  dimensions: {
    id: {
      sql: `id`,
      type: `number`,
      primaryKey: true
    },
    
    nombre: {
      sql: `nombre`,
      type: `string`
    }
  },
  
  dataSource: `default`
});
